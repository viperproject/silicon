
predicate smaller10(j : Int){
    j < 10
}


predicate greater0(i : Int)
{
    i >= 0 && smaller10(i)
}

method my_unfold(n : Int)
    requires greater0(n)
    ensures n >= 0
{
    assert unfolding greater0(n) in (unfolding smaller10(n) in n >= 4)
}