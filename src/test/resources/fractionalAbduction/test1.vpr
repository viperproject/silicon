field next: Ref

predicate list(x: Ref) {
    x != null ==> acc(x.next, 1/2) && acc(list(x.next), 1/2)
}

method foo(x: Ref)
  requires acc(x.next, 1/2) //wilcard)
  //requires acc(list(x.next), 1/4)
{ 
  // fold acc(list(x), 1/2)
  assert acc(list(x), 1/2)
}
