field l: Ref
field r: Ref

predicate tree(x: Ref){
    x != null ==> acc(x.l) && tree(x.l) && acc(x.r) && tree(x.r)
}


method test(x: Ref, y: Ref, z: Ref)
{
    x.l := y
    x.r := z
    assert tree(x)
}
