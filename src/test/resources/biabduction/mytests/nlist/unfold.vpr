field next: Ref

predicate list(x: Ref){
    x != null ==> acc(x.next) && list(x.next)
}


method test(x: Ref)
    requires list(x)
    requires x != null
    ensures acc(x.next)
    ensures list(x.next)
{
}