// Spec quality: 1
// Proof quality: 1/3
// Progress: 1/3

function sum(x: Int, y: Int): Int
  requires x >= 0 && y >= 0
  ensures result >= 0       // PQ: 4/6
{
    x + y
}

method client()
{
    var x: Int, y: Int
    assume x > 10
    assume y > 0

    x := sum(x, y)          // PQ: 0/2
}