predicate ThreeFives(x: Int, y: Int) {
  x == 3 &&
  y == 5
}

method use(a: Int, y: Int)
  requires ThreeFives(a,y)
{
  var x: Int := 3
  unfold ThreeFives(a,y)

  fold ThreeFives(x,y)

  unfold ThreeFives(x,y)

  assert y == 5

  fold ThreeFives(x,y)
}