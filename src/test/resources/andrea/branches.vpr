
field f: Int

method foo(a: Ref, b: Ref)
  requires acc(a.f) && acc(b.f)
{
  var n:Int, c: Bool

  assume a.f > 0
  assume b.f > 0

  if(c){
    n := a.f
  }else{
    n := b.f
  }

  var x: Int := 0
  if(a.f > n){
    x := a.f + b.f
  }else{
    x := n
    assert x >= 1
  }
  assert x > 0
}