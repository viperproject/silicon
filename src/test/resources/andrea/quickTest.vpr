field f: Int

method maybeAlias(a: Ref, b: Ref, c: Bool, n: Int)
    requires acc(a.f, 1/2) && acc(b.f, 1/2)
    requires c ==> a == b
    requires a.f > 0 && n > 0
{
    if(c){
        a.f := n + 1
    }
    assert a.f >= 0
}