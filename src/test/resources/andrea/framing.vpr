

field f: Int

method foo(a: Ref) returns(n:Int)
  requires acc(a.f, 1/2)
  ensures acc(a.f, 1/2) && n == a.f
{
  n := a.f
}

method framing1(a: Ref)
  requires acc(a.f) && a.f > 0
{
  var n: Int
  n := foo(a)
  assert n > 0
  a.f := 5
}
