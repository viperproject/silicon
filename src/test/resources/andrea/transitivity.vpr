
method level2(n: Int) returns(a:Int)
  requires n > 0
  ensures a > 0
{
  a := 2 * n
}

method level1(n: Int) returns(a:Int)
  requires n > 0
  ensures a > 0
{
  a := level2(n)
}

method client(n: Int)
  requires n > 0
{
  var a: Int 
  a := level1(n)
  assert a > 0
}