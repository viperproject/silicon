import "./nnlist.vpr"

/*
method test(x: Ref)
    //requires acc(x.next)
    //requires list(x.next)
    //ensures list(x)
{
}
*/
/*
method test(x: Ref)
    //requires acc(x.next)
    //requires list(x.next)
{
    assert list(x)
}
*/


method test(x: Ref, y: Ref)
//requires acc(x.next)
//requires list(y)
//ensures list(x)
{
    x.next := y
    assert list(x)
}


/*
method test(x: Ref, y: Ref)
//requires acc(x.next)
//requires list(y)
//ensures list(x)
{
    x.next := y
}
*/