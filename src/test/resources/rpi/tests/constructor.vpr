field next: Ref

method main()
{
    var x: Ref
    var i: Int

    x := null
    i := 10

    while (i > 0)
    {
        var t: Ref
        t := new(next)
        t.next := x
        x := t
        i := i - 1
    }

    while (x != null)
    {
        x := x.next
    }
}