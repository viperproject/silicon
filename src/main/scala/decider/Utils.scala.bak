package semper
package silicon
package decider

object utils {
  val prefix = "$Type.name."

  def toType(clazz: ast.Class): String = prefix + clazz.fullName
  def toType(channel: ast.Channel): String = prefix + channel.fullName
}