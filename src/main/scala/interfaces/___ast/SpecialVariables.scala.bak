package semper.syxc
package interfaces.ast

import ast.{Variable, Type}

object specialVariables {
  val thisId = "this"
  def thisVar(typ: Type) = Variable(thisId, typ)

	val resultId = "result"
	def resultVar(typ: Type) = Variable(resultId, typ)
}